import React from 'react';
import FileUploadForm from "../../Components/FileUploadForm/FileUploadForm";

const Main = () => {
	return (
		<div className={'App'}>
			<FileUploadForm/>
		</div>
	);
};

export default Main;